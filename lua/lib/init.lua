---@class VinLib
local M = {
    copy = require("lib.copy"),
    colors = require("lib.colors"),
    files = require("lib.files"),
    git = require("lib.git"),
    ui = require("lib.ui"),
    config = require("lib.config"),
    lsp = require("lib.lsp"),
}

return M
